Seed assets folder

Place product images in:
`backend/src/database/seeders/assets/product_images`

Supported formats: `.jpg`, `.jpeg`, `.png`, `.webp`

If the folder is empty, product media seeding is skipped.
